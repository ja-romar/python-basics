import numpy as np

#First create an array
u = np.array([1,0])

print(u)

#We crate a seccond array
v = np.array([0,1])

print(v)

#We can add two arrays

z = np.add(u, v)
print(z)

#this operation its equivalate to a vector add